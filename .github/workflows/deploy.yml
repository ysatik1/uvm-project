name: Deploy to GitHub Pages

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Create index.html
        run: |
          echo '<!DOCTYPE html>
          <html>
          <head>
            <meta charset="UTF-8">
            <title>Учебная Виртуальная Машина</title>
            <style>
              body { font-family: Arial; padding: 40px; text-align: center; }
              h1 { color: #2c3e50; }
              .btn { display: inline-block; padding: 15px 30px; margin: 10px; 
                     background: #3498db; color: white; text-decoration: none; 
                     border-radius: 5px; font-size: 18px; }
              .btn:hover { background: #2980b9; }
            </style>
          </head>
          <body>
            <h1>🎓 Учебная Виртуальная Машина</h1>
            <p>Веб-версия интерпретатора УВМ</p>
            <a href="uvm_web.html" class="btn">▶ Открыть интерпретатор</a>
          </body>
          </html>' > index.html
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
